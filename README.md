## BestOJ v1.0.0
感谢选用 BestOJ！您现在所下载的版本为：**BestOJ v1.0.0**。
本次更新新增 25 个函数，累计 **39 个函数**！包含一个快速安装包，请检查你所下载的版本。
---
### 新增函数（25个）
| 分类 | 新增函数 |
|------|----------|
| **数组操作** | `maxVal()`, `minVal()`, `sumArr()`, `avgArr()`, `reverseArr()`, `countOccur()` |
| **字符串操作** | `toUpper()`, `toLower()`, `strLen()`, `strReverse()`, `isAnagram()`, `countVowels()`, `countConsonants()` |
| **数学工具** | `absInt()`, `max3()`, `min3()`, `isEven()`, `isOdd()`, `sqr()` |
| **位运算** | `isPowerOfTwo()`, `countBits()`, `reverseBits()` |
| **日期工具** | `isLeapYear()`, `daysInMonth()`, `isValidDate()` |
---
### 全部函数（39个）
| 分类 | 函数 |
|------|------|
| 幂运算 | `power(a, b)` |
| 数论 | `gcd(a, b)`, `lcm(a, b)`, `isPrime(n)` |
| 取模 | `pmod(a, b, mod)` |
| 进制转换 | `tBase(n, base)`, `fBase(n, base)` |
| 排列组合 | `Permu(n, m)`, `Combi(n, m)`, `fact(n)` |
| 数列 | `fib(n)` |
| 回文/数字 | `isPalind(n)`, `numSum(n)`, `rvsNum(n)` |
| 数组 | `maxVal()`, `minVal()`, `sumArr()`, `avgArr()`, `reverseArr()`, `countOccur()` |
| 字符串 | `toUpper()`, `toLower()`, `strLen()`, `strReverse()`, `isAnagram()`, `countVowels()`, `countConsonants()` |
| 数学 | `absInt()`, `max3()`, `min3()`, `isEven()`, `isOdd()`, `sqr()` |
| 位运算 | `isPowerOfTwo()`, `countBits()`, `reverseBits()` |
| 日期 | `isLeapYear()`, `daysInMonth()`, `isValidDate()` |
---
### 说明
- 本版本为正式稳定版
- 新增 25 个函数，功能更丰富
- 后续将继续增加更多实用函数

## ENG VER
## BestOJ v1.0.0
Thank you for choosing BestOJ! The version you are currently downloading is: **BestOJ v1.0.0**.
This release adds 25 new functions, bringing the total to **39 functions**! Includes a quick installation package. Please check the version you have downloaded.
---

### New Functions (25)
| Category | New Functions |
|----------|---------------|
| **Array Operations** | `maxVal()`, `minVal()`, `sumArr()`, `avgArr()`, `reverseArr()`, `countOccur()` |
| **String Operations** | `toUpper()`, `toLower()`, `strLen()`, `strReverse()`, `isAnagram()`, `countVowels()`, `countConsonants()` |
| **Math Tools** | `absInt()`, `max3()`, `min3()`, `isEven()`, `isOdd()`, `sqr()` |
| **Bitwise Operations** | `isPowerOfTwo()`, `countBits()`, `reverseBits()` |
| **Date Tools** | `isLeapYear()`, `daysInMonth()`, `isValidDate()` |
---
### All Functions (39)
| Category | Functions |
|----------|-----------|
| Power | `power(a, b)` |
| Number Theory | `gcd(a, b)`, `lcm(a, b)`, `isPrime(n)` |
| Modulo | `pmod(a, b, mod)` |
| Base Conversion | `tBase(n, base)`, `fBase(n, base)` |
| Permutation & Combination | `Permu(n, m)`, `Combi(n, m)`, `fact(n)` |
| Sequence | `fib(n)` |
| Palindrome / Digit | `isPalind(n)`, `numSum(n)`, `rvsNum(n)` |
| Array | `maxVal()`, `minVal()`, `sumArr()`, `avgArr()`, `reverseArr()`, `countOccur()` |
| String | `toUpper()`, `toLower()`, `strLen()`, `strReverse()`, `isAnagram()`, `countVowels()`, `countConsonants()` |
| Math | `absInt()`, `max3()`, `min3()`, `isEven()`, `isOdd()`, `sqr()` |
| Bitwise | `isPowerOfTwo()`, `countBits()`, `reverseBits()`` |
| Date | `isLeapYear()`, `daysInMonth()`, `isValidDate()` |
---
### Notes
- This is a stable release
- 25 new functions added, more feature-rich
- More useful functions will be added in future updates
